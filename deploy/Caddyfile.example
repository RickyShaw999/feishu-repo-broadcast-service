{$PUBLIC_HOST} {
	encode gzip

	handle /health {
		reverse_proxy app:8080
	}

	handle /ready {
		reverse_proxy app:8080
	}

	handle /webhooks/* {
		reverse_proxy app:8080
	}

	respond "feishu-repo-broadcast-service" 200
}
